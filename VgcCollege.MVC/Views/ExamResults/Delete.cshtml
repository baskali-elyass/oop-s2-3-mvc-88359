@model VgcCollege.Domain.ExamResult
@{ ViewData["Title"] = "Delete Exam Result"; }
<h2>Delete Exam Result</h2>
<div class="card p-4" style="max-width:500px">
    <div class="alert alert-danger">Delete result for <strong>@Model.StudentProfile.Name</strong> on <strong>@Model.Exam.Title</strong>?</div>
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <div class="d-flex gap-2"><button type="submit" class="btn btn-danger">Delete</button><a asp-action="Index" asp-route-examId="@Model.ExamId" class="btn btn-outline-secondary">Cancel</a></div>
    </form>
</div>
